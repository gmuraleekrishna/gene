import { FemaleFilterPipe } from './female-filter.pipe';

describe('FemaleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FemaleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
