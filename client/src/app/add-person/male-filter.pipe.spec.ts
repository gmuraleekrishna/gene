import { MaleFilterPipe } from './male-filter.pipe';

describe('MaleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MaleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
